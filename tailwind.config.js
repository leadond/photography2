/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      colors: {
        // Custom colors can be added here
      },
      fontFamily: {
        // Custom fonts can be added here
      },
      spacing: {
        // Custom spacing values can be added here
      },
      borderRadius: {
        // Custom border radius values can be added here
      },
      boxShadow: {
        // Custom shadow values can be added here
      },
      animation: {
        // Custom animations can be added here
      },
      keyframes: {
        // Custom keyframes can be added here
      },
    },
  },
  plugins: [],
}
